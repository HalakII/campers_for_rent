export function selectLocation(state) {
  return state.filters.location;
}

export function selectTransmission(state) {
  return state.filters.transmission;
}

export function selectForm(state) {
  return state.filters.form;
}

export function selectDetails(state) {
  return state.filters.details;
}
